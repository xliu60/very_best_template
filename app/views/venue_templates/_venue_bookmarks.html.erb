<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Bookmarks

    <a data-toggle="collapse" href="#venue_bookmarks_new_form">
      Add bookmark
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @venue.bookmarks.each do |bookmark| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/bookmarks/<%= bookmark.id %>">
          <%= bookmark.notes %>
        </a>

        <%if current_user == bookmark.user %>
          <a href="/delete_bookmark_from_venue/<%= bookmark.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="venue_bookmarks_new_form">
        <%= render partial: "venue_templates/venue_bookmarks_new_form" %>
      </div>
    </div>

  </ul>
</div>
