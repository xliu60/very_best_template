<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All bookmarks
    </h1>

    <a href="/bookmarks/new" class="btn btn-block btn-outline-secondary">
      Add a new bookmark
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Dish
        </th>

        <th>
          Venue
        </th>

        <th>
          User
        </th>

        <th>
          Notes
        </th>

        <th>
          Image
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @bookmarks.each do |bookmark| %>
      <tr>
        <!-- Display bookmark.id start -->
        <td>
          <%= bookmark.id %>
        </td>
        <!-- Display bookmark.id end -->

        <!-- Display bookmark.dish_id start -->
        <td>
          <% if bookmark.dish.present? %>
            <a href="/dishes/<%= bookmark.dish.id %>">
              <%= bookmark.dish.name %>
            </a>
          <% end %>
        </td>
        <!-- Display bookmark.dish_id end -->

        <!-- Display bookmark.venue_id start -->
        <td>
          <% if bookmark.venue.present? %>
            <a href="/venues/<%= bookmark.venue.id %>">
              <%= bookmark.venue.name %>
            </a>
          <% end %>
        </td>
        <!-- Display bookmark.venue_id end -->

        <!-- Display bookmark.user_id start -->
        <td>
          <% if bookmark.user.present? %>
            <a href="/users/<%= bookmark.user.id %>">
              <%= bookmark.user.username %>
            </a>
          <% end %>
        </td>
        <!-- Display bookmark.user_id end -->

        <!-- Display bookmark.notes start -->
        <td>
          <%= bookmark.notes %>
        </td>
        <!-- Display bookmark.notes end -->

        <!-- Display bookmark.image start -->
        <td>
          <img src='<%= bookmark.image %>' class='img-fluid'>
        </td>
        <!-- Display bookmark.image end -->

        <!-- Display bookmark.created_at start -->
        <td>
          <%= time_ago_in_words(bookmark.created_at) %> ago
        </td>
        <!-- Display bookmark.created_at end -->

        <!-- Display bookmark.updated_at start -->
        <td>
          <%= time_ago_in_words(bookmark.updated_at) %> ago
        </td>
        <!-- Display bookmark.updated_at end -->

        <td>
          <a href="/bookmarks/<%= bookmark.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @bookmarks, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
